"use client";

import Sidebar from "./Sidebar";

/**
 * Menüleiste wie auf Desktop: links immer sichtbar, aufklappbar (Mobile + Desktop).
 */
export default function SidebarWrapper() {
  return <Sidebar />;
}
